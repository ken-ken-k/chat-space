.Side-bar
  .Side-bar__user-name
    .Side-box
      .Side-box__name 
        = current_user.name
      .Side-box__icons 
        = link_to "#" do
          = icon('far', 'edit')
      .Side-box__icon
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')
  .Side-bar__group-list
    .Sid-bar__box
      = link_to "#", class: "box" do
        .Side-bar__box__name techcamp
        .Side-bar__box__text まだメッセージがありません